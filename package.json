{
  "name": "aquasight",
  "version": "0.2.0",
  "private": true,
  "description": "AquaSight / ya xian zhi",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "once": "node src/run.js --once",
    "digest": "node src/digest.js --once",
    "start": "node src/server.js",
    "db:migrate": "node src/store/migrate.js",
    "deploy": "npx wrangler deploy --config worker/wrangler.toml"
  },
  "dependencies": {
    "entities": "^8.0.0",
    "fast-xml-parser": "^5.11.1",
    "htmlparser2": "^12.0.0"
  }
}
