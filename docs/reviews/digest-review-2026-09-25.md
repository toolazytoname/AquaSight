---
type: synthesis
sources:
  - ../../src/bark.js
  - ../../src/budget.js
  - ../../src/digest.js
  - ../../src/pipeline.js
  - ../../.github/workflows/digest.yml
---

# 9 月 25 日早报推送复核

## 已确认

- GitHub Actions 仓库变量 SITE_URL 仍为旧 workers.dev 域名。已在本次复核中更新为 https://quack.weichao.ren ，并读回验证；新域名首页返回 HTTP 200。没有重发通知。
- 推送 URL 直接使用站点根地址，没有跳转早报的 `/#/digest` 路由。
- `digestOnce` 从已有事件挑选并推送，没有发送前的中文标题补全。标题缺少 `titleZh` 时直接回退到原文。
- `src/digest.js` 在 `digestOnce` 返回后才生成 AI 综述；`buildDigestPayload` 也没有使用综述。
- 采集运行 36099091677（2026-09-25T05:33:53Z）日志出现 74 次 `BUDGET_CANDIDATES`、3 次 `invalid:not-object`；这些是该轮输出记录数，不是服务商请求量。
- 代码的每日候选上限为 100；每次 reserve（包括某些重试）增加计数，零单价也受限。不能据此认定服务商额度耗尽。
- 当日 digest 工作流原计划北京时间 08:05，实际于 UTC 04:23:01（北京时间 12:23:01）启动。是否此前由 collect 补发，仍需核对通知记录。

## 建议优先级

1. 推送深链到早报；发送前补全中文、校验输出，翻译失败时使用合格备选稿，避免直接回退整句英文。
2. 免费模型单独按实际请求数、token、429 和服务商重置时间管理额度。为早报预留额度，批量翻译与长文整理分开，不再让低优先级整理占满标题翻译预算。
3. 先整理、生成综述、校验，再发布和推送。针对空输出 / 非 JSON 的模型返回增加诊断；不把模型输出直接当事实。
4. 选稿增加事件时间与报道时间区分。`Nokia Design Archive (2025)` 可能是旧内容重新受到关注，需要核查原始日期与当天新增事实，不能仅凭标题年份认定过时。
5. “乘车离开白宫”缺少独立新闻信息量；优先选有结果或影响的报道，没有合格公共新闻时留空。星链地面站疑似纵火应结合正文判断归为科技还是公共安全。
6. 压缩宣传式长标题，保留“报告称”“疑似”等限定；每条加一句有来源支持的新事实，解释 TenPayGo 等仅报产品名的标题。推送保持短，细节放早报页。
7. 处理定时任务延迟：核对补发链路，并让刊期、截止时间和实际送达时间保持一致。

## 免费额度待核对

模型与接口保存在 GitHub Secrets，不能读回；已向用户询问 free 模型的服务商和型号。现阶段能确认本地上限已触发，不能确认上游每日总额、剩余额度或实际使用型号。

容量估算：早报 10 条标题批量处理为 1–2 次请求，加 1 次综述，约 2–3 次/日（不含重试与来源核查）；现有全量整理每天四轮、每轮候选池最多 100 条，未命中缓存时最多有 400 个候选处理机会，实际受每日 100 次门槛约束。两者应分别统计。
