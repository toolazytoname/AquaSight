# 任务交付说明

对照用户批次。完成实现、测试、说明后才勾选。缺凭证或观察期未完成的保持未完成。

测试：`npm test`（137 通过，含 P1 回归）。`tests/browser-flow.test.js` 是本地 HTTP 阅读流，不再写仓库报告。复审通过前，不把 T01–T23 当作已验收。T24/T25 仍未完成。

## 第一批

- [x] **T01** 回归夹具在 `tests/fixtures/regression/`：重复华为新闻、不同 OpenAI 新闻、2024 讣告、不同公司财报。`tests/regression.test.js` 离线复现，不打外网、不发真实 Bark。
- [x] **T02** 文章 ID = 来源+规范化 URL+外部 ID 的哈希；事件 ID = 持久 `evt:` UUID。`stableCardId` 不再用公司名。同事件加源 ID 不变，输出无重复 ID。
- [x] **T03** 聚类同时看主体、动作、产品、日期。取消「同实验室+发布词就合并」和 Jaccard 连锁。同公司不同发布、不同公司财报、不同日期同标题独立；有产品证据的中英文同事件可合并。
- [x] **T04** 普通讣告、事故、旧闻回顾不 breaking、不即时推送。「张三去世」「十年前地震回顾」不 breaking。英文正式技术发布可进候选（`notifyEligible`）。
- [x] **T05** 同轮按 ID 去重、按价值排序；HTTP 与 Bark `code` 都成功才记已发送。5xx 有限重试；超时记 `unknown` 不永久漏发。任务重启去重仍有效。

## 第二批

- [x] **T06** 36氪文章/快讯分开；HN 保留点赞、评论、文章和讨论链接；GitHub 角色是开源发现，不再叫趋势；热搜 `role=clue`。源健康含成功、用途、最后成功时间。
- [x] **T07** `fast-xml-parser` + `htmlparser2` + `entities`。不再截成 120 字。正文只给精选按需抽。失败保留可读原文，仓库名不被拆。
- [x] **T08** 科技 60% / 商业 30% / 公共 ≤10%。娱乐、猎奇、讣告、促销隐藏。分值：相关 35%、影响 25%、新信息 20%、来源 15%、热度 5%。银行新闻不因 36氪进科技栏。
- [x] **T09** 首页 30 条配额 18/9/3；单源 ≤6、同主体 ≤3。早报 10 条 6/3/1。隐藏内容不进早报。公共不足不凑数。
- [x] **T10** SpaceXAI（`XAI_API_KEY` + `https://api.x.ai/v1`，模型 `grok-4.5`）。输出校验；不足则 `insufficient`，不按标题编造；企业自述进 `attribution`。无凭证自动降级。
- [x] **T11** 月 100 元、日 3.3 元、每天最多 100 篇。内容哈希+版本缓存。预留费用后调用。并发不能突破预算。
- [x] **T12** HN 引用的 X 链接、手工导入链接和摘录。账号订阅只留适配接口。无凭证显示未接通。不用免费镜像。

## 第三批

- [x] **T13** `worker/` + D1 schema；`npm start` / `npm run db:migrate` / `npm run deploy`。采集和模型留在 Actions/Node。
- [x] **T14** 文章、事件成员、偏好、反馈、已读、收藏、任务、缓存、通知。`/api/v1` 列表/详情/早报/设置/导入/状态。见 `docs/api.md`。
- [x] **T15** Access 邮箱与采集 `INGEST_TOKEN` 分开。导入限制协议、大小、超时、重定向，拦截内网和云元数据。
- [x] **T16** 隐藏、降权、屏蔽来源、撤销。Web/早报/Bark 共用偏好。08:05 早报、即时 ≤3、23:00–08:00 静默。限额按北京日写进已发送记录，跨重启有效。即时默认关闭。
- [x] **T17** 重叠锁、超时、有限重试、原子快照、早报补执行、幂等。失败保留上次有效内容。诊断见 `/api/v1/status`。
- [x] **T18** 原始 7 天、事件 30 天、映射 90 天，收藏独立快照。导出/恢复。不再把 `sent.json` 挂到站点根。旧 `events.json` 继续写。旧 ID 不强行映射。首轮静默基线。

## 第四批

- [x] **T19** 导航：精选、最新、早报、收藏。首页即精选。中文标题、概述、来源、时间。不展示内部英文规则和裸分数。桌面侧栏，手机底栏。
- [x] **T20** `#/event/:id` 可直达。要点、来源、不同报道、讨论。原文标题可展开。返回恢复筛选和滚动。
- [x] **T21** 收藏、分享、反馈撤销、已读、搜索、过滤、刷新、分页。加载/空/失败/部分源失败/缓存陈旧都有状态。刷新保留筛选。失败有 toast。
- [x] **T22** 无衬线、中性底、克制绿、深浅主题。触控 ≥44px，键盘可聚焦。PWA 缓存壳和最近内容。退出清私人缓存。长标题 `overflow-wrap: anywhere`。

## 第五批

- [x] **T23** 工程回归覆盖身份、聚类、旧闻、跨语言、过滤、预算、异常模型、采集失败、通知失败与重启。`tests/browser-flow.test.js` 走精选→详情→搜索→收藏。无 Chromium，未截真实视口图。
- [ ] **T24** 评审入口 `#/review` 已提供。尚未由用户确认 30 条喜欢/不喜欢，7 天影子运行未开始。不得标记达标，不得自动开启即时推送。
- [ ] **T25** 文档已更新（README、本文、`docs/api.md`、`docs/deploy.md`）。尚未配置 Cloudflare、Access 身份、模型凭证，未做国内网络实测，**未上线**。达标并配好凭证后再切 Web/Bark。移动客户端尚未选定。
